--TestingAssetNewHorizionsModel.asset
local sceneHelper = asset.require('util/scene_helper')

local TestKeybindings = {
    {
        Key = "F8",
        Name = "play test recording",
        Command = "openspace.sessionRecording.startPlayback('${BASE}/OpenSpaceVisualTesting/OpenSpaceVisualTesting/NewHorizions/RecordingNewHorizionsModel.osrecording')",
        Documentation = " ",
        Local = true
    },
}

asset.onInitialize(function()
    Keys = sceneHelper.bindKeys(TestKeybindings)
end)

asset.onDeinitialize(function ()
    sceneHelper.unbindKeys(TestKeybindings)
end)

asset.export("TestingKeybindings", TestKeybindings)
